config BR2_PACKAGE_KERNEL_TESTS
	bool "kernel-tests"
	help
	  Test scripts for verifying kernel features on the
	  Fairphone 2: CPU DVFS frequency scaling, thermal
	  management, and IOMMU operation. Outputs results
	  in TAP v13 format.
